<%= form_with url:"/applications/#{@application.id}", method: :patch, local: true do |f|%>
    <%= f.label :reason, "Tell us why you want to adopt:"%>
    <%= f.text_area :reason%>
    <%= f.submit "Submit Application", method: :patch, params: {status: "pending"} %>
    <% end %>
